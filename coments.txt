Егор, привет!
Спасибо большое за оперативное ревью кода.
Прошу принять проект без изменений. Понял все недочеты и иправлю их в свободное время.

По поводу 6 задания твой комментарий "Наверно можно было в индексе поискать номер продажи."
к сожалению в модели данных нидекс sales состоит из vin и номера строки, а в метод revert_sale подается номер продажи sales_number
class Sale(BaseModel):
    sales_number: str
    car_vin: str
    sales_date: datetime
    cost: Decimal

    def index(self) -> str:
        return self.car_vin
в общем невозможно сделать поиск индексу Поэтому и пробегаю весь файл с продажами
Я побоялся менять класс, вдруг поплывут тесты.

Я разогнался в только к 7 заданию, как ты видел написал приватный метод _get_model_info 
и применил его в методе top_models_by_sales

Очень понравилось твой подход по переходу к нужной строке (target_string * (500 + len("\n"))) 

Еще раз большое спасибо 